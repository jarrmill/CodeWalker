# Subproject node_modules and build artefacts are ignored in their own .gitignore files.
# This root ignore covers anything that might land at the repo root.
node_modules
.env
.DS_Store
.mcp.json
.nuxt
